"use strict"

//initialize total variable
let f=32

//get 3 scores from user and add them together
const fahrenheit= parseInt(prompt("Enter Fahrenheit temperature"));
const celsius= (32)


const result= parseFloat(prompt(total/3));

const html= `<p>Score 1 =${Fahrenheit}</p>
             <p>Average score= ${average.toFixed(1) }</p>`;
        document.write(html);